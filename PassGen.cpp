#include <iostream>
#include <time.h>

int main ()
{
	int size; //stores the size of the password
	//prompt for the size;
	std::cout << "Please enter the length of the password: ";
	std::cin >> size;
	//stores everything in an character array
	char* pass = new Char[size];
	for (int i = 30; i < 40; i++)
	{
		char [0] = i;
		for (int j = 30; j < 40; j++)
			...
	}
	return 0;
}

char* genPass (char* pass)

